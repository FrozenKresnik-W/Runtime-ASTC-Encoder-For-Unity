Mobile platform compress texture to astc format at runtime for Unity3D.

Cross-compile based on [astc-encoder ](https://github.com/ARM-software/astc-encoder)

Use C# P/Invoke to call the native plugin